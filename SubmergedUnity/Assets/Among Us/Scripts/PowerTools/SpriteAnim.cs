using UnityEngine;

namespace PowerTools
{
	[DisallowMultipleComponent]
	public class SpriteAnim : SpriteAnim_POWERTOOLS
	{
		// The SpriteAnim class in base Among Us is in the Assembly-CSharp namespace, so we need to replicate that here.
	}
}
